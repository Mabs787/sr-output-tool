{
  "name": "sr-output-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Screen Reader Outcome Engine — shared screen reader logic for the browser extension",
  "workspaces": [
    "packages/sr-engine",
    "packages/sr-extension"
  ],
  "scripts": {
    "build": "npm run build:engine && npm run sync:extension-engine",
    "build:engine": "npm run build -w packages/sr-engine",
    "sync:extension-engine": "npm run sync:runtime -w packages/sr-extension",
    "package:extension": "npm run build && npm run package:zip -w packages/sr-extension",
    "test:unit": "npm run build:engine && npm run sync:extension-engine && node --test tests/*.test.mjs",
    "test-app": "node test-app/server.js"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
